# create a list of integers
numbers = [12, 5, 19, 8, 3, 15, 7]

# find the largest and smallest numbers
largest = max(numbers)
smallest = min(numbers)

# print the results
print(f"Largest number: {largest}")
print(f"Smallest number: {smallest}")

